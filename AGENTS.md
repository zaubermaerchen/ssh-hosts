# Repository Guidelines

## プロジェクト構成

このリポジトリは Go 1.24 製の小さな CLI です。エントリーポイントと CLI テストは `main.go`、`main_test.go` に置きます。OpenSSH 設定の読み込み・評価は `internal/sshconfig/`、組み込みの対話選択は `internal/finder/` が担当します。各パッケージのテストは実装と同じディレクトリの `*_test.go` に追加してください。

## ビルド・テスト・開発コマンド

- `go build .`: CLI をビルドします。
- `go test ./...`: 全パッケージのテストを実行します。
- `go vet ./...`: よくある Go の不具合を静的に検査します。
- `gofmt -w <files>`: 変更した Go ファイルを整形します。
- `go run . --json`: ローカル設定を使って JSON 出力を確認します。実データを避ける場合は設定ファイルのパスを末尾に指定してください。

## コーディング規約

Go 標準の書式と慣例に従い、インデントや import は `gofmt` に任せます。公開名は `CamelCase`、非公開名は `camelCase`、パッケージ名は短い小文字にします。SSH 設定の解析、接続先の評価、対話選択、表示形式は分離し、CLI 層に解析ロジックを増やさないでください。エラーには対象パスやオプションなど、原因を特定できる文脈を含めます。

## テスト方針

標準の `testing` パッケージを使用し、テーブル駆動テストを優先します。テスト名は `Test<対象>_<条件>` を基本とします。`Include` の再帰処理、glob、ワイルドカード除外、先勝ち評価、IPv6、JSON、対話選択のキャンセルなど、境界条件と失敗経路も検証してください。変更後は最低でも `go test ./...` と `go vet ./...` を実行します。

## コミットとプルリクエスト

コミットは一つの論理変更に絞り、命令形の短い件名を付けます。プルリクエストには目的、主な変更、実行した検証、互換性への影響を記載してください。CLI 出力を変更する場合は、変更前後の例と README の更新を含めます。

## サブエージェントの利用

軽微でないコード変更では、親エージェントは必ずサブエージェントを利用してください。

- 不明な実装、依存関係、影響範囲の調査は `explorer` に委譲します。
- 実装やテスト追加は、担当ファイル、完了条件、検証方法を明示して `coder` に委譲します。
- 実装後は `reviewer` に、回帰、境界条件、互換性、セキュリティ、テスト不足を確認させます。
- 独立した作業は並列化し、担当範囲を重複させません。共有ワークツリー上の他者の変更を取り消してはいけません。
- 軽微な文言修正や単純な一行変更では、委譲は必須ではありません。

設計、公開 API、データ形式、セキュリティ上の判断は親エージェントが保持します。親エージェントは成果を鵜呑みにせず差分を確認し、統合後のテストを実行してから完了を報告してください。
